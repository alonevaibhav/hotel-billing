import 'package:flutter/cupertino.dart';

class SelectDish extends StatelessWidget {
  const SelectDish({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
